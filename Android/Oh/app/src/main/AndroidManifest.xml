<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"/>

    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_st_launcher_round"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_st_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data android:name="com.samsung.android.icon_container.has_icon_container" android:value="true"/>
        <activity
            android:name=".Splash"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterActivity">
        </activity>
        <activity android:name=".Main"></activity>
        <activity android:name=".DetailViewActivity"></activity>
        <activity android:name=".DetailViewActivity2"></activity>
        <activity android:name=".Setting1Activity"></activity>
        <activity android:name=".Setting2Activity"></activity>
        <activity android:name=".Setting3Activity"></activity>

        <service
            android:name=".RealService"
            android:enabled="true"
            android:exported="true" />

        <service
            android:name=".RestartService"
            android:enabled="true"
            android:exported="true" />

        <receiver android:name=".AlarmRecever"/>

        <receiver
            android:name=".RebootRecever"
            android:enabled="true"
            android:exported="false"
            android:process=":remote">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>